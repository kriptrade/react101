function AnimalShow({title}) {
    // 1 .. Version
    //  const title = props.title;
    // const handle = props.handle;

    // 2 .. Version
    // const { title, handle } = props;

    return (
        <div>Cow!</div>
    );
}

export default AnimalShow;